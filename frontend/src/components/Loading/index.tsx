import React from 'react';

import { Container } from './styles';

const Loading: React.FC = () => {
  return (
    <Container>
      <h1>Loading</h1>
    </Container>
  );
};

export default Loading;
